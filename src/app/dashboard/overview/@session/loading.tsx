import { SessionCardSkeleton } from '@/features/overview/components/current-meeting-card-skeleton';
import React from 'react';

export default function Loading() {
  return <SessionCardSkeleton />;
}
